> Public archive note: application/process names are aliases. Host-side paths are
> sanitized; board runtime paths are retained. The frozen test-image BUILD_ID is
> intentionally public for reproducibility.

# PM 裁决台账

本表只记录会改变交付判读或证据等级的 PM 裁决。实验原始记录不因裁决被抹除；后续文档
用带日期追注说明现行口径。批准人统一记为 `PM`。

| 发现编号 | 日期 | 裁决 | 理由 | 批准人 |
|---|---|---|---|---|
| P0-6 / IDENTITY-WAIVER | 2026-09-03 | Demo 阶段接受既有提交身份与 annotated tag 未签名；正式 release 前统一处理提交身份与仓库可见性策略 | 当前 Demo 验证的是可复现证据链；重写历史会破坏已经引用的提交与标签身份，签名/可见性属于正式发布治理 | PM |
| GST-P50-GATE | 2026-09-04 | 驳回把 gst 业务门从 p99 改为 p50 | p50 可见是需要报告的补充观察，但原合同比较量是 p99；看结果后换门会改变问题定义 | PM |
| GST-MORE-REPEATS | 2026-09-04 | 驳回为改变当前结论而追加重复数 | 已完成批次按固定三重复合同报告；新增重复必须成为独立事前合同的新批次，不能并入旧批次刷数 | PM |
| S4-N3-MEDIAN | 2026-09-03 | B 组按 profile 分别使用三重复中位与发布中心 `±5 pp`；`n=3` 最多吸收一个离群 | 同 seed 不固定 arena 指派，单重复可出现约 1 MiB 台阶；分档中位保留 profile 差异且不把回收字节误列为确定性项 | PM |
| GST-P99-DIRECTION | 2026-09-03 | gst p99 方向为 `REPORT_ONLY`；只把 nearest-rank、重复中位、none 离散带和严格比较的正确执行设为硬门 | 不同板上可能得到不同方向；方向本身是批次业务观察，不应被 workflow 误报为协议失败 | PM |
| V4-1 / V4-CALIBRATION | 2026-09-04 | v4 A 带降级为“校准带”；撤回“GBS 重基线通过”和“GBS 为 HQ 首选 L2”，等待 held-out 验证 | GBS 观测参与中心与半宽的构造，用同一观测落带不能提供独立通过证据 | PM |
| V4-1 / HELDOUT-CRITERION | 2026-09-04 | 以事前提交并打轻量 tag 的 GBS ELF × mixed/medium-only × 2 重复作为独立 held-out；四格不并入建带样本，按既有 v4 闭区间逐格判定，4/4 落带才通过，任一带外即不通过且不得改带或补跑刷数 | 只有在校准带冻结后、与建带样本隔离的观测才能独立检验 GBS 路径 | PM |
| V4-1 / GBS-PROMOTION | 2026-09-04 | held-out 四格 4/4 落带且身份、健康、清理门全过后，恢复“GBS 重基线通过”，GBS 转为默认 L2 路径；冻结件保留为可审计备选 | [`gbs-heldout-contract-20260904`](../tools/runners/gbs_heldout_validation_20260904/contract.json) 固定的四格均未参与建带并独立通过，关闭 V4-1 的循环论证缺口 | PM |
| B2-EVIDENCE-LEVEL | 2026-09-04 | B2 实际执行日订正为 2026-09-04；没有独立事前 commit/tag 的“预登记”降级为“固定合同重放” | 原始纳秒时间戳确定实际日期；同一工作轮内先写规格不足以形成第三方可审计的事前凭证 | PM |
| V5-RPM-DEPENDENCY | 2026-09-04 | 默认 verify 的测试 fixture 不得要求 RPM/GBS/ARM 工具链；两个 GBS 环境测试改用自包含桩实际执行，不以缺包静默通过 | N4-01 已把真实 GBS 构建移出默认路径；验证该行为的测试不能反向引入未声明的 RPM 系统依赖 | PM |
| V5-PREDELIVERY-12 | 2026-09-04 | 交付就绪门固定为三种远端克隆形态 × GBS 可发现/不可发现 × RPM 工具链可发现/不可发现，共 12 次完整 verify；最小形态只要求 Git、Python 3 与基础系统 userland | 同时覆盖交付身份与可选工具存在性，才能防止干净 Ubuntu/Debian 环境再次被测试夹具阻断 | PM |

## 2026-09-07 第四轮裁决：demo-v6 → demo-v7

| 发现编号 | 日期 | 裁决 | 理由 | 批准人 |
|---|---|---|---|---|
| N6-01 / V6-02 | 2026-09-07 | 显式 `gbs` 必须完整生成并核验 RPM 与三个 ELF；环境不可用、锁超时、产物缺失均非零退出；root 属主 buildroot 清理失败单列 `REPORT_ONLY` | 静态门通过不能代替真实构建；清理失败不否定已校验产物 | PM |
| V6-01 / V6-PREDELIVERY-15 | 2026-09-07 | 最小 PATH 改为显式命令白名单，依赖逐项披露/检查；增加损坏 rpmspec/gbs 形态，共三克隆 × 五 PATH = 15 次完整 verify；另归档一次真实 GBS 构建通过 | 排除法没有隔离未声明命令；工具存在但不可用也必须覆盖 | PM |
| N6-02 / V6-1 | 2026-09-07 | 可选 rpmspec 缺失、执行异常、超时或非零退出均带原因降级；Python 静态 spec/%files/manifest 仍为硬门 | 可选 RPM 环境不得阻断默认 verify | PM |
| N6-03 | 2026-09-07 | held-out 四格限定为 alloc_bench；GBS gst/reclaim_probe 的行为依据为此前 9/3 retry2，优先补公开其已有 gst 紧凑件 | 不把 A 锚点独立验证扩大为三个工具的独立板上验证 | PM |
| V6-2 / GBS-STATUS | 2026-09-07 | 构建记录保留历史命令并注明不等于复跑命令；复跑以 checker 的固定 source commit/唯一 buildroot 为准；pending 改为带范围的事实状态并保留追注 | 使历史身份记录与当前操作指南各自可审计 | PM |
| V6-5 / V6-6 / V6-7 | 2026-09-07 | 披露 B/B2 unsorted 取值层级差异与保守方向；估算器/落点追注日期统一为 9/4 并恢复按终审裁决订正来历；INDEX 补报告与裁决入口 | 只修文档口径/可发现性，不改数据或结论 | PM |
| N6-04 / V6-4 | 2026-09-07 | 后续事前合同用 annotated tag，保留 tagger 时间戳与推送完成时间；按建议采用 ≥10 分钟审计间隔，短于该值须开跑前裁决，结果记录实际间隔 | 合同存在时间与首次板端操作之间可核验；既有轻量标签原样保留 | PM |

以上 v6→v7 只修交付工具行为与文档；不改变任何测量、验收带、held-out 独立性或技术
结论。逐项实现与验证见 [`第 4 轮闭环`](review_fix_20260903.md#第-4-轮终审闭环demo-v62026-09-07)。

对应修复闭环见 [`review_fix_20260903.md`](review_fix_20260903.md)，从 demo-v2 开始的提交与
证据变化索引见 [`changes_since_demo_v2.md`](changes_since_demo_v2.md)。

## 2026-09-07 第五轮裁决：demo-v7 → demo-v8

| 发现编号 | 日期 | 裁决 | 理由 | 批准人 |
|---|---|---|---|---|
| N7-01 | 2026-09-07 | checker 取锁后、调用 GBS 前采集 clean HEAD/dirty 状态/入口与 checker 哈希/Python/UTC；publisher 只校验并原样搬运，拒绝缺失或状态不一致；干净提交重构建，旧记录保留并标明替代 | publisher 事后补写的入口哈希不能绑定执行时刻，旧入口为提交前工作树版本 | PM |
| N7-02 | 2026-09-07 | 锁不可写、RPM 工具损坏、未知 GBS 失败为 NOT-EVALUATED 非零；明确源码编译缺陷为 FAIL；缺 RPM/任一 ELF 维持硬 FAIL RC=1 | 区分环境不可用与产物缺陷；旧裁决中缺产物口径以本条订正 | PM |
| N7-03 | 2026-09-07 | 白名单每项必须解析为真实可执行文件，函数/别名不能冒充 | command -v 成功不足以证明外部命令存在 | PM |
| N7-02b / N7-06 / V6-8 / V6-9 / F12 | 2026-09-07 | 标题限定 alloc_bench held-out；packaging 唯一入口与现行路径统一；固定/移动 repo 分组复核；导语按带中心取两位；披露同板性局限 | 收紧对客表述与可复现操作，不扩大技术证据 | PM |

本轮仅交付工具自证机制与文档修正，不改测量、验收带或技术结论；历史 demo-v7 保留。

## 2026-09-07 第六轮裁决：demo-v8 → demo-v9

| 发现编号 | 日期 | 裁决 | 理由 | 批准人 |
|---|---|---|---|---|
| N8-01 | 2026-09-07 | 执行前记录 proof 字节 SHA；GBS 后重读磁盘、先字节再 JSON；输出和发布副本也复核；改写/截断/删除/符号链接替换 RC=1 | 校验内存对象无法发现构建期磁盘 proof 被改写，publisher 拒绝不能弥补入口误报 PASS | PM |
| N8-02 | 2026-09-07 | 预检通过 Python PATH 解析真实可执行文件，不依赖 shell command；新增环境标记防递归 | command 与目标同时被函数覆盖可绕过旧预检；异常自调用须在启动子程序前停止 | PM |
| CC N8-01 | 2026-09-07 | 两份 GBS 配置、manifest、packaging/*.spec 按 HEAD Git 字节哈希加入自证覆盖 | porcelain 不保证发现 skip-worktree 隐藏的输入改动 | PM |
| N8-03 / V8-1 | 2026-09-07 | 缺头文件归未知/需人工二判，不归环境；诊断优先级公开 | 可能是源码/BuildRequires 缺陷，也可能是环境缺失，单行日志不能定性 | PM |
| CC N8-02/N8-03 / N8-04 | 2026-09-07 | 声明自记记录而非签名级远程证明；原始 GBS 日志哈希由 checker 记录并校验；入口哈希不是调用链证明；脏快照用语义明确标签 | 约束完整性校验的能力边界，不扩大证据强度 | PM |

本轮仅交付工具校验加固与文档，不改任何测量或结论。`demo-v8` 保留；交付前仍须
三克隆 × 五白名单 PATH 的 15 次完整 verify。实现与验证见
[`第 6 轮闭环`](review_fix_20260903.md#第-6-轮终审闭环demo-v82026-09-07)。

## 2026-09-08 第七轮裁决：demo-v9 → demo-v10

| 发现编号 | 日期 | 裁决 | 理由 | 批准人 |
|---|---|---|---|---|
| Codex N9-01 | 2026-09-08 | 启动文件清除标记后留下未导出函数属于交付门槛；入口须在普通命令前拒绝注入，真实 Python execve 净化 shell；预置净化标记失败关闭 | 环境变量检查不等于 shell 函数表检查，必须消除分析器未执行却静默 PASS | PM |
| Kimi V9-1 / CC N9-02 | 2026-09-08 | 缺命令与函数/启动文件注入分开诊断；保留全量函数拒绝并公开 Environment Modules 的干净进程规避命令 | 正常 HQ 主机需要可操作提示，不能把 module 等辅助函数误解释为缺依赖 | PM |
| CC N9-01 | 2026-09-08 | 自签 proof 可整体伪造判为交付门槛之外，正式 release 处理签名/远程证明 | 三家均确认不影响 HQ 正常使用；当前机制不声称对抗恶意 host 的签名级证明 | PM |
| F01 / F05 | 2026-09-08 | 媒体与完整原始件包外交付判为交付门槛之外；正式 release 决定完整件公开策略 | 三家均确认不影响 HQ 正常使用；现有获取位置与 SHA 核验路径保留 | PM |
| V10-DELIVERY-MATRIX | 2026-09-08 | 三克隆 × 六环境共 18 次完整 verify，注入形态先做五变体预期拒绝；保留 demo-v9，切 annotated demo-v10 | 正常复算必须真执行，拒绝探针与 OVERALL PASS 分开记录 | PM |

本轮仅入口执行环境净化与文档，不改任何测量、验收带、held-out 或技术结论。
闭环见 [`第七轮修复`](review_fix_20260903.md#第-7-轮终审闭环demo-v92026-09-08)。

## 2026-09-08 定向裁决：demo-v10 → demo-v11

| 发现编号 | 日期 | 裁决 | 理由 | 批准人 |
|---|---|---|---|---|
| N10-01 / 原 N9-01 | 2026-09-08 | demo-v10 判 NOT-FIXED，暂不交付；入口拒绝不得依赖可被函数遮蔽的 shell exec/exit；由最终真实 Python 调用自然传回退出状态 | exec+exit 同时遮蔽时，虽无假 PASS 且检测有效，仍为空输出 RC=0，足以使 HQ 自动化误判 | PM |
| N10-01 ENUM | 2026-09-08 | builtin 本身可遮蔽；枚举根必须先验证，枚举失败不能当作无函数 | 函数表不可可信采集时须失败关闭 | PM |
| N10-01 TEST/MATRIX | 2026-09-08 | exec、exit、两者组合、builtin 与未导出/自删除/预置标记组合逐例断言 RC=2、明确非空诊断、无 MODE/PASS、冒充命令零调用；扩展远端三形态注入探针 | 仅检查没有假 PASS 输出不能证明拒绝状态正确 | PM |
| V11-SCOPE | 2026-09-08 | 修复先进 main；保留 demo-v10，切 annotated demo-v11；本轮仅入口信任边界与注入拒绝路径加固，不改测量或结论 | 沿用既有证据与验收带，不新增板端操作 | PM |

前节 v10 五变体通过的历史记录保留，但不再代表 N9-01 已闭合；本次以
[`N10-01 修复记录`](review_fix_20260903.md#第-8-轮定向闭环demo-v102026-09-08)
及扩展的交付前矩阵为准。

## 2026-09-08 系统级补测续跑裁决

| 发现编号 | 日期 | 裁决 | 理由 | 批准人 |
|---|---|---|---|---|
| SYSTEM-OCCUPANCY | 2026-09-08 | 确认测试板当前专供本 glibc 项目，无其他人在用；批准清除 PID 26799/pts/0 与 27105/pts/1，前后快照归档 | 两者为残留登录会话；闭合首次占用停止门，不豁免身份/环境/健康门 | PM |
| SYSTEM-RESUME | 2026-09-08 | 沿用原 annotated contract tag，不改矩阵、采集或派生定义；先闭合执行器测试，再跑 21 格；任一真实停止门阻断后续集成/交付 | 续跑不是调参或重选样本；完整矩阵与交付检查通过前保留 demo-v11 | PM |

执行原文与 parser 自采集误报的离线订正见 [报告 §5](system_level_before_after_20260908.md#5-pm-裁决后续跑占用处置与执行器闭合)。
